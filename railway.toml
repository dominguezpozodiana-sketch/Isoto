[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:5000 app:app"
# Se eliminó la directiva inválida para dejar que Railway maneje los reinicios por defecto
